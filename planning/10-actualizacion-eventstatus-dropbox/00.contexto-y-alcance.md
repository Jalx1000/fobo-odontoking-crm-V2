# Actualización SMD `eventStatus` — Contexto, bugs y alcance

**Fecha de análisis:** 2026-07-14
**Origen:** Mensaje de Mauri Pache (SMD) del 13/07/2026 18:07
**Estado del cambio en SMD:** ✅ **YA DESPLEGADO** (verificado contra Dropbox real el 14/07)
**Estado del CRM:** ✅ **BUG-1, BUG-2, BUG-3, BUG-5 y BUG-12 corregidos** (14/07) · ✅ **BUG-4 y BUG-11 corregidos** (14/07, 2ª tanda) — ver [Parte 6](#parte-6--qué-se-corrigió)
**Retractado:** ❌ **BUG-10 no existe** — se diagnosticó contra la carpeta de pruebas; los ids sí cruzan
**Pendiente:** backfill de 19 citas colgadas del doctor 458, BUG-6, BUG-7, BUG-8, BUG-9

---

## TL;DR (leer esto si no lees nada más)

1. SMD ya desplegó su cambio: ahora **cada modificación de una cita genera un archivo nuevo** en la carpeta Dropbox del día en curso, con un atributo nuevo `eventStatus: CREATED | UPDATED`.
2. **El `eventStatus` no nos hace falta.** El CRM ya distingue "crear" de "modificar" por el `_id`, que se mantiene igual entre archivos. Es un dato de confirmación, no la solución.
3. **El problema real es otro:** los archivos viejos **no se borran**. El sync recorre las carpetas de la más nueva a la más vieja, y como "el último archivo procesado gana", **el archivo viejo pisa al nuevo y revierte la modificación**.
4. Consecuencia hoy, con datos reales: la cita `6a54f35…` está el **17 de julio** en SMD y el CRM la va a mostrar el **21 de julio**.
5. El arreglo es acotado: **ordenar por `updated_at` y no aplicar payloads viejos**. No depende de ninguna respuesta de SMD.
6. Hay **2 bugs preexistentes en `DropboxService`** que pueden tumbar el sync entero y que conviene arreglar en el mismo lote.
7. Queda **1 pregunta abierta para Mauri**: dónde viene el médico. En los 4 archivos actuales no está.

---

## Parte 1 — Cómo funciona el sync hoy (contexto necesario)

Sin esto, el resto no se entiende. Hay **dos caminos** que hacen lo mismo por vías distintas:

### Camino A — El cron (el que importa para este análisis)

```
app/Console/Kernel.php
  └─ cada 5 min: smd:sync-dropbox --days=2   (si el sync no está pausado)
       └─ SmdAppointmentSyncService::run()
            └─ para cada día (hoy, hoy-1):
                 DropboxService::listFilesForDate()  → lista archivos JSON de esa carpeta
                 DropboxService::downloadJson()      → baja cada archivo
                 IncomingAppointmentService          → crea/actualiza/cancela Lead + Activity
```

### Camino B — El webhook

```
POST /api/webhooks/dropbox  (Dropbox avisa que algo cambió)
  └─ DropboxWebhookController::handle()  → valida firma HMAC
       └─ ProcessDropboxNotification (job)
            └─ DropboxService::listChanges($cursor)  → cambios desde el último cursor
```

### Cómo decide crear vs. actualizar

Tabla `smd_synced_events`, con `external_id` (= el `_id` del JSON) **único**:

| Situación | Qué hace |
|---|---|
| No existe fila con ese `_id` | **Crea** Lead + Activity |
| Existe y el hash del payload es idéntico | **Ignora** (sin cambios) |
| Existe y el hash cambió | **Actualiza** la cita |
| `archived: true` o `status` cancelado | **Cancela** el Lead |

> 🔑 **Lo importante:** la decisión de crear vs. actualizar **ya se toma por `_id`**, no por ningún campo de estado. Por eso `eventStatus` no cambia la lógica.

---

## Parte 2 — Qué cambió en SMD

### El mensaje de Mauri (13/07, 18:07)

> Igual te comento como será el cambio, en la respuesta de cada cita/evento tendrán un atributo **"eventStatus"** el cual tendrá 2 valores, **CREATED** o **UPDATED**.
>
> Si el valor es CREATED sabrán que la cita es nueva y podrán crearla en su CRM, pero si el valor es UPDATED, sabrán que la cita ya fue creada y está siendo modificada, entonces podrán con el "id" modificarla en su CRM.
>
> Entonces, si una cita/evento es creada ó ahora es modificada también, **aparecerá un nuevo evento en la carpeta de dropbox del día en curso**.

### Evidencia real — qué hay hoy en la carpeta

Carpeta configurada (`DROPBOX_APPOINTMENTS_FOLDER`): `/smd-events/69bd99417549b10008e0ab5f`

| Archivo | `_id` | `eventStatus` | `summary` | `startDate` | `updated_at` |
|---|---|---|---|---|---|
| `event-2026-07-13-1416-6a54f35…` | `6a54f35…` | **(no trae)** | Creado 21 de Jul | 21 jul | 07-13 14:16:50 |
| `event-2026-07-14-0141-6a54f35…` | `6a54f35…` | UPDATED | Updated 16 de Jul | 16 jul | 07-14 01:41:31 |
| `event-2026-07-14-0143-6a55944…` | `6a55944…` | CREATED | Created VIE 17 | 17 jul | 07-14 01:43:41 |
| `event-2026-07-14-0144-6a54f35…` | `6a54f35…` | UPDATED | Updated from 16 to 17 de Jul | **17 jul** | 07-14 01:44:50 |

**Son 2 citas repartidas en 4 archivos.** La cita `6a54f35…` tiene 3 archivos: la creación del 13, y dos modificaciones encadenadas del 14 (la movieron 21 → 16 → 17 de julio).

### Qué se confirma con esto

| Pregunta | Respuesta | Cómo se verificó |
|---|---|---|
| ¿El cambio ya está desplegado? | **Sí.** El archivo del 13 a las 14:16 no trae `eventStatus`; los tres del 14 sí. SMD lo desplegó esa noche. | Descarga real |
| ¿El `_id` se mantiene entre CREATED y UPDATED? | **Sí.** Los 3 archivos comparten `_id: 6a54f3524e365b0008cba3e3` y `eventId: 1783952187466`. | Descarga real |
| ¿Riesgo de citas duplicadas? | **No.** Al ser el `_id` estable, la dedup por `external_id` sigue siendo correcta. | Deducido de lo anterior |
| ¿Los archivos viejos se borran? | **No.** El del 13 sigue ahí. | Descarga real |
| ¿Puede haber varios archivos del mismo evento en la misma carpeta? | **Sí.** `0141` y `0144` son el mismo `_id`, mismo día. | Descarga real |

---

## Parte 3 — Bugs

Severidad: 🔴 crítico (rompe datos o el sync) · 🟠 importante · 🟡 menor

Cada bug indica si fue **verificado ejecutándolo** o **deducido leyendo el código**.

---

### 🔴 BUG-1 — El archivo viejo revierte la modificación *(verificado)*

**Ubicación:** `packages/Webkul/Admin/src/Services/SmdAppointmentSyncService.php:47-48`

**Qué pasa:** las modificaciones de citas **nunca llegan al CRM**. Queda el dato original.

**Por qué:** el bucle recorre los días desde el más nuevo hacia atrás:

```php
for ($i = 0; $i < $days; $i++) {
    $date = $baseDate->copy()->subDays($i)->toDateString();  // i=0 → HOY, i=1 → AYER
    $files = $this->dropbox->listFilesForDate($date);
    foreach ($files as $file) { $this->processFile($file, $summary, $onLog); }
}
```

Procesa **la carpeta más nueva primero y la más vieja al final**. Como la dedup es "el último archivo procesado gana", la secuencia real con los datos de hoy es:

1. Carpeta del **14** → `0141` (crea la cita) → `0144` (la actualiza a **17 jul**) ✅ correcto
2. Carpeta del **13** → el archivo viejo **sigue ahí** → su hash difiere del guardado → lo trata como "un cambio más" y **reaplica el payload viejo encima** → vuelve a **21 jul** ❌

**Impacto concreto hoy:** en SMD la cita `6a54f35…` está el **17 de julio**; el CRM la mostrará el **21 de julio**. Cuatro días de diferencia en una cita real.

**Por qué es traicionero:** mañana (15/07) la carpeta del 13 sale de la ventana de `--days=2` y **la cita se autocorrige sola**. El dato está mal solo durante la ventana del `--days` — el tiempo justo para que alguien lo vea mal y después no se pueda reproducir.

**Verificación:** se reprodujo con un test temporal usando un payload calcado del real (mismo `_id`, archivo CREATED el 13 + UPDATED el 14, cron con `--days=2`):

```
1. tras CREATED del 13              → "Creado 21 de Jul" | 2026-07-21 09:00
2. tras UPDATED del 14 (--days=2)   → "Creado 21 de Jul" | 2026-07-21 09:00   ← la modificación NO entra
3. cron 5 min después               → "Creado 21 de Jul" | 2026-07-21 09:00   ← sigue sin entrar
```

**Nota adicional:** dentro de un mismo día el orden es **pura suerte**. Hoy Dropbox devolvió `0141` antes que `0144` (cronológico, sale bien), pero la API de Dropbox **no garantiza ese orden**.

---

### 🔴 BUG-2 — Un fallo transitorio de Dropbox deja el sync muerto 3h30m *(verificado)*

**Ubicación:** `packages/Webkul/Admin/src/Services/DropboxService.php:23-48`

**Qué pasa:** si Dropbox falla **una sola vez** al renovar el token, el sync queda caído 3 horas y media, **incluso después de que Dropbox se recupera**.

**Por qué:** `token()` envuelve el refresh en `Cache::remember(..., 12600, ...)`, pero el closure **también retorna en el camino de error**:

```php
return Cache::remember(self::TOKEN_CACHE_KEY, self::TOKEN_TTL, function () {
    // ...
    if (! $refreshToken || ! $appKey || ! $appSecret) {
        return config('smd.dropbox.access_token') ?? '';   // ← línea 29: se cachea el fallo
    }
    // ...
    if (! $response->successful()) {
        Log::error('[Dropbox] Error refrescando token', ...);
        return config('smd.dropbox.access_token') ?? '';   // ← línea 41: se cachea el fallo
    }
    // ...
});
```

Cache no distingue éxito de error: guarda lo que devuelva el closure, incluido el string vacío, por 12600 segundos.

Peor: `retryWithFreshToken()` hace `Cache::forget()` y vuelve a llamar a `token()`, que **re-envenena el cache** con el mismo string vacío.

**Verificación:** con un test temporal, simulando un `500` del endpoint de token:

```
token tras fallo                      → ''
valor cacheado                        → ''
token tras recuperación de Dropbox    → ''   ← sigue vacío
```

---

### 🔴 BUG-3 — `listChanges()` lanza `TypeError` y pierde la notificación *(verificado)*

**Ubicación:** `packages/Webkul/Admin/src/Services/DropboxService.php:165-205`

**Qué pasa:** el webhook revienta y la notificación de Dropbox **se pierde**.

**Por qué:** el método está declarado `: array`, pero llama a `retryWithFreshToken()` **sin pasar `default:`**, así que hereda `$default = null`. Si el token sigue expirado tras el reintento, retorna `null` → PHP lanza `TypeError` en el `return`.

```php
public function listChanges(string $cursor): array
{
    $result = $this->retryWithFreshToken(function (string $token) use ($cursor) { ... });
    //                                   ↑ falta   , default: ['entries' => [], ...]
    return $result;  // ← TypeError si $result es null
}
```

`listFilesForDate` (`default: []`) y `downloadJson` (`default: null`) **sí lo pasan**. `listChanges` es el único que se olvidó.

**Impacto:** rompe `ProcessDropboxNotification::handle()` en la línea 42, **antes de guardar el cursor**. El job reintenta 3 veces, falla, y esa notificación se pierde.

---

### 🟠 BUG-4 — El médico no se identifica; se crea un doctor "Recepcionista" *(verificado — PENDIENTE)*

> **⚠️ ACTUALIZACIÓN 14/07 — pregunta resuelta, pero el problema cambió de forma.**
>
> El usuario confirmó que **el médico sí viene en `attendances[]` cuando se lo selecciona** en SMD. Se verificó contra Dropbox real: el archivo `event-2026-07-14-1127-6a54f35…` (`__v=5`) trae dos asistentes — `Juan Perez` (paciente) y `Susana Urrusti` (la doctora).
>
> **Pero hay un patrón nuevo y crítico:** los payloads `UPDATED` vienen **deshidratados**.
>
> | Archivo | `__v` | `eventStatus` | attendances |
> |---|---|---|---|
> | `…07-13-1416` | 0 | (no trae) | `Juan Perez [type=patient, phone=12345678]` |
> | `…07-14-0141` | 3 | UPDATED | `Juan Perez [type=AUSENTE, phone=AUSENTE]` |
> | `…07-14-0143` | 0 | CREATED | `Juan Perez [type=patient, phone=12345678]` |
> | `…07-14-0144` | 4 | UPDATED | `Juan Perez [type=AUSENTE, phone=AUSENTE]` |
> | `…07-14-1127` | 5 | UPDATED | `Juan Perez [AUSENTE]` \| `Susana Urrusti [AUSENTE]` |
>
> Es decir: **`CREATED` trae `type` y `phone`; `UPDATED` no trae ninguno de los dos.** Esto coincide con el hallazgo de mayo ya documentado en `05/00.preguntas-pendientes.md` (Pregunta 4, "attendances sin phone").
>
> **Consecuencias para el arreglo del médico:**
> 1. **No se puede usar `type` para distinguir médico de paciente** — en los UPDATED no existe. Hay que identificar al médico cruzando el `_id` del asistente contra `doctors.unique_id`.
> 2. **No se puede caer al `owner`** cuando no se identifica al médico: el `owner` es quien creó el evento (la recepcionista), no el profesional.
> 3. **`resolvePerson()` tampoco puede confiar en el `phone`** en los UPDATED. Si un paciente fue matcheado por teléfono, nunca se le guardó el `smd_patient_id`, así que un UPDATED deshidratado no lo encuentra y **crearía un paciente duplicado**.
>
> Esto invalida la Pregunta 3 de la Fase 05 (`owner._id` como `unique_id` del doctor), que se decidió cuando el `owner` sí era el médico.

**Ubicación:** `packages/Webkul/Admin/src/Services/IncomingAppointmentService.php:216-219`

**Qué pasa:** el CRM crea un doctor llamado **"Recepcionista Odontoking"** y le cuelga las citas.

**Por qué:** `normalizeDropbox()` busca el médico en `attendances[]` con `type: physician|doctor`. Si no lo encuentra, cae al `owner`:

```php
if (! $physicianRaw) {
    $owner = $this->parseOwner($data['owner'] ?? null);
    $physicianRaw = $owner['_id'] ? ['_id' => $owner['_id'], ...] : null;
}
```

**En los 4 archivos actuales NO hay ningún médico:** `attendances[]` trae solo al paciente, y `owner` es siempre `Recepcionista Odontoking` con `type: "entity:staff"`.

**Verificación:**
```
doctor asignado a la cita → id 458 | "Recepcionista Odontoking" | unique_id 69bd9a6a7549b10008e0ac6b
```

**⚠️ Contexto histórico importante:** según `planning/05-sincronizacion-citas-dropbox/00.preguntas-pendientes.md` (Pregunta 3, ✅ respondida), la decisión fue **usar `owner._id` como `unique_id` del doctor** — porque en los JSON de mayo **el `owner` ERA el médico**:

```json
// JSON de mayo/2026 — owner = MÉDICO
"owner": { "_id": "6525b4de...", "name": "Nuevo", "lastName": "Medico", "phone": "76123456" }

// JSON de julio/2026 — owner = RECEPCIONISTA
"owner": { "_id": "69bd9a6a...", "name": "Recepcionista", "lastName": "Odontoking", "type": "entity:staff" }
```

Es decir: **la semántica de `owner` cambió** (o al menos, ahora refleja *quién creó el evento*, no *de quién es la cita*). El campo `type: "entity:staff"` es nuevo y permite distinguirlo. **Esto invalida parcialmente la decisión de la Fase 05 y es lo que hay que consultar con Mauri.**

---

### 🔴 BUG-12 — Una cancelación que además edita el título pierde la edición *(verificado — CORREGIDO 14/07)*

**Reportado por el usuario el 14/07:** canceló la cita `6a562447af…` y en el mismo movimiento le editó el título a `"test editado"`. El CRM canceló el lead correctamente pero **se quedó con el título viejo**.

SMD manda **un solo archivo** con la cancelación y la edición juntas:

```
event-2026-07-14-1310-6a562447af…  __v=2  UPDATED  archived=false  status="CANCELED"  summary="test editado"
```

La rama de cancelación de `applyPayload()` llamaba a `cancelDropbox()` y retornaba, **sin aplicar nunca el contenido** ni guardar el `raw_payload`. Efecto secundario: `source_updated_at` avanzaba a la versión nueva mientras `raw_payload` quedaba en la vieja, dejando la fila inconsistente.

**Corregido:** la rama de cancelación ahora aplica `updateDropbox()` antes de cerrar el lead (solo si hay `activity_id`, porque sin él `updateDropbox()` crearía una cita nueva para un evento ya cancelado) y persiste el `raw_payload`.

Verificado sobre la cita real: `ACTIVITY 40922` pasó de `test` a `test editado`, conservando `stage=6`, `status=0` y `lost_reason='Cita cancelada en SMD'`.

---

### 🟠 BUG-5 — Sin protección de concurrencia *(deducido, no verificado)*

**Ubicación:** `app/Console/Kernel.php:19-21` y `packages/Webkul/Admin/src/Jobs/ProcessDropboxNotification.php:16`

El cron corre cada 5 min **sin `withoutOverlapping()`**, y el webhook despacha el job **sin `ShouldBeUnique`**. Ambos leen `smd_synced_events` y llaman a `processDropbox()`.

Si coinciden: los dos ven `$existing = null`, **los dos crean Lead + Activity**, y recién ahí el índice único de `external_id` hace fallar el segundo `insert` — que se cuenta como "error" mientras el Lead y la Activity duplicados **ya quedaron creados y huérfanos**.

Con webhook + cron cada 5 minutos, es cuestión de tiempo.

---

### 🟠 BUG-6 — Una cita reactivada queda "perdida" para siempre *(deducido, no verificado)*

**Ubicación:** `IncomingAppointmentService.php:133-139` (cancel) vs. `:88-110` (update)

`cancelDropbox()` pone `status = 0`, `lost_reason` y `closed_at`. Si en SMD reactivan esa cita, `updateDropbox()` solo corrige `lead_pipeline_stage_id` — **nunca revierte `status` a 1 ni limpia `closed_at` / `lost_reason`**. El lead vuelve al stage correcto pero sigue marcado como perdido.

---

### ❌ BUG-10 — ~~Los `_id` de `attendances[]` viven en otro espacio de identificadores~~ *(FALSO — retractado el 14/07)*

> **🚨 ESTE BUG NO EXISTE. El diagnóstico fue erróneo.**
>
> **Causa del error:** se diagnosticó descargando de la carpeta **de pruebas**
> (`/smd-events/69bd99417549b10008e0ab5f`), que era la que tenía el `.env` en ese momento.
> Esa carpeta contiene entidades **descartables creadas por SMD para probar el cambio de
> julio**: un "Juan Perez" y una "Adriana Soria" con ids inventados que, lógicamente, no
> cruzan con nada nuestro.
>
> **Verificación contra la carpeta REAL** (`/smd-events/60ccc528c1c48400065e3861`),
> 30 archivos / 52 asistentes:
>
> | Chequeo | Resultado |
> |---|---|
> | `attendances[]._id` → `doctors.unique_id` | **23 matches** |
> | `attendances[]._id` → `persons.smd_patient_id` | **28 matches** |
> | Sin match | **1** |
> | **Physicians sin match** | **0** |
>
> **Los ids SÍ cruzan.** El `_id` de `attendances[]` es el mismo id de SMD que guardamos.
>
> **Corolario:** como el `_id` matchea, **no hace falta `type`** para distinguir médico de
> paciente — alcanza con ver en qué tabla cae el id. La deshidratación de los `UPDATED`
> deja de ser un bloqueo y **no hay que pedirle nada a SMD**.
>
> **Lección:** verificar siempre contra qué carpeta apunta `config('smd.dropbox.appointments_folder')`
> antes de sacar conclusiones de los datos. `DropboxService` la lee **en el constructor**, así que
> un `config([...])` en runtime sobre una instancia ya resuelta **no la cambia** (eso hizo que un
> primer intento de comparar ambas carpetas devolviera los mismos archivos).

---

<details>
<summary>Diagnóstico original (incorrecto, se conserva como registro)</summary>

**Descubierto:** 2026-07-14, al intentar arreglar el cambio de paciente reportado por el usuario.

Los `_id` de `attendances[]` **sí son estables** entre ediciones (Adriana Soria es `69d904e42930f5000853aa7c` en las 3 versiones del evento `6a562447af…`). El problema es que **no corresponden a los identificadores que guarda el CRM**:

| Persona | `_id` en `attendances[]` | Lo que guarda el CRM |
|---|---|---|
| Adriana Soria (doctora) | `69d904e42930f5000853aa7c` | `doctors.unique_id = 69122c77bdb3c90008178d37` |
| Paciente | `6a19eb6f7994000009e8050b` | `persons.smd_patient_id = 6a3d465e49b7260008a2c6f9` |

Ninguno cruza. Verificado con `whereIn('unique_id', $ids)` y `where('smd_patient_id', $id)`: **cero coincidencias**. Los 30 doctores tienen `unique_id` poblado, así que no es un problema de datos faltantes — son **espacios de ID distintos**.

El `unique_id` de los doctores viene del `physician._id` de la API de calendario (`/api/calendar/schedule/availability`); el `smd_patient_id` viene de la API de pacientes (`/api/patients`). Los `attendances[]` de Dropbox usan un tercer identificador.

**Por qué funcionó igual la cita del 14/07:** `resolveDoctor()` la resolvió por **nombre exacto** (paso 2), no por id. Fue suerte de que el doctor se llame igual en ambos lados.

**Consecuencia:** en un payload `UPDATED` (sin `type` y sin `phone`) el CRM **no tiene ninguna clave fiable** para identificar quién es el médico y quién el paciente, ni para matchearlos contra sus registros locales.

**Opciones:**
- **(a) Pedir a SMD que hidrate los `UPDATED`** igual que los `CREATED` (con `type` y `phone`). Es un cambio de una línea de su lado y el código actual funcionaría sin tocar nada. **Recomendada.**
- **(b) Aprender el mapeo** desde los `CREATED` hidratados: guardar el `_id` del attendance en una columna nueva (`smd_attendance_id` en `doctors` y `persons`) cuando llega un payload hidratado, y usarla en los `UPDATED`. Funciona, pero tiene período de calentamiento (solo cubre eventos creados después del cambio) y requiere migración.

> Ninguna de las dos hizo falta: los ids ya cruzaban. Ver la retractación arriba.

</details>

---

### 🟠 BUG-11 — Un cambio de paciente o doctor en SMD no se refleja *(verificado — PENDIENTE)*

**Reportado por el usuario el 14/07:** cambió el paciente de la cita `6a562447af…` (`Alejandro…` → `Fabiana Morales Lopez`, archivo `__v=1`) y el CRM siguió mostrando el paciente anterior.

`IncomingAppointmentService::updateDropbox()` (`:74-118`) solo actualiza `title`, `comment`, `schedule_from` y `schedule_to` de la Activity, más el stage del Lead. **Nunca re-resuelve `participants` ni `doctor_activities`.** Si en SMD cambian la persona o el profesional de la cita, el CRM se queda con los originales, en silencio.

Está bloqueado por **BUG-10**: aunque se agregue la re-resolución, no hay clave con la que matchear a los asistentes de un payload `UPDATED`.

---

### 🟡 BUG-7 — `listFilesForDate` ignora la paginación *(deducido)*

**Ubicación:** `DropboxService.php:110`

Lee `entries` pero **descarta `has_more`**. Hoy no duele con carpetas de un día, pero si la estructura de carpetas crece, empieza a perder archivos en silencio. `listChanges` sí pagina.

---

### 🟡 BUG-8 — El webhook loguea la firma esperada *(deducido)*

**Ubicación:** `DropboxWebhookController.php:51`

Loguea `signature_expect`, el HMAC esperado del body. Quien tenga acceso a los logs puede forjar una petición válida para ese body. Debería salir del log.

---

### 🟡 BUG-9 — `.env.example` no tiene ninguna variable de Dropbox/SMD *(verificado)*

Faltan `SHAREMEDATA_API_KEY`, `SHAREMEDATA_PATIENTS_URL`, `DROPBOX_APP_KEY`, `DROPBOX_APP_SECRET`, `DROPBOX_REFRESH_TOKEN`, `DROPBOX_APPOINTMENTS_FOLDER`. Es la acción A3 del plan v2, que quedó pendiente. Tropiezo garantizado en el próximo despliegue.

---

## Parte 4 — Qué falta / preguntas abiertas

### Para Mauri (SMD) — afecta a BUG-4

> **¿Los `UPDATED` van a seguir viniendo sin `type` ni `phone` en `attendances[]`?**
>
> Los archivos `CREATED` traen el asistente completo (`type: "patient"`, `phone`, `birthday`, `age`), pero los `UPDATED` traen solo `_id`, `name`, `lastName`, `fullName` e `id`. Sin `type` no hay forma directa de saber cuál asistente es el médico y cuál el paciente; sin `phone` no se puede matchear al paciente existente.
>
> Si es un efecto colateral del cambio y pueden hidratar los `UPDATED` igual que los `CREATED`, el mapeo se simplifica mucho. Si no, lo resolvemos por nuestro lado cruzando los `_id` contra nuestra tabla de doctores.

### Confirmado — ya NO hace falta preguntar

- ~~¿El `_id` o el `eventId` es el identificador estable?~~ → **Ambos son estables.** El CRM usa `_id`, está bien.
- ~~¿SMD borra el archivo viejo al modificar?~~ → **No lo borra.** Confirmado.
- ~~¿Hay riesgo de citas duplicadas de cita?~~ → **No** (el `_id` no cambia).
- ~~¿Dónde viene el médico?~~ → **En `attendances[]`, cuando se lo selecciona.** El `owner` es quien creó el evento, no el médico.

---

## Parte 5 — Alcance del arreglo

### En alcance

| # | Cambio | Bug | Archivo |
|---|---|---|---|
| 1 | Ordenar los archivos de **todo el rango de días** por `updated_at` ascendente antes de procesar, y **descartar** todo payload cuyo `updated_at` sea anterior o igual al ya guardado | BUG-1 | `SmdAppointmentSyncService.php` |
| 2 | Persistir `updated_at` (o `source_updated_at`) en `smd_synced_events` para poder comparar | BUG-1 | migración nueva |
| 3 | No cachear el token cuando el refresh falla; cachear solo el éxito | BUG-2 | `DropboxService.php` |
| 4 | Pasar `default:` en `listChanges()` | BUG-3 | `DropboxService.php` |
| 5 | `withoutOverlapping()` en el cron + `ShouldBeUnique` en el job | BUG-5 | `Kernel.php`, `ProcessDropboxNotification.php` |
| 6 | Tests que cubran los 3 escenarios críticos (ver abajo) | — | `tests/Feature/` |

**Por qué `updated_at` y no `eventStatus`:** el arreglo por `updated_at` es inmune al orden en que Dropbox devuelva los archivos, funciona **también con el archivo viejo del 13 que no trae `eventStatus`**, y no depende de que SMD sea consistente con el valor que manda. `eventStatus` queda como verificación secundaria (loguear si dice `CREATED` pero el `_id` ya existía, que indicaría una desincronización).

### Tests que deben cubrirlo

1. Cita creada ayer + modificada hoy, cron con `--days=2` → **gana la modificación** (hoy falla).
2. Dos archivos del mismo `_id` en la misma carpeta, devueltos por Dropbox **en orden inverso** → gana el de `updated_at` mayor.
3. Archivo sin `eventStatus` (legacy, como el del 13) → se sigue procesando bien.

### Fuera de alcance (decidir aparte)

- **BUG-4** (médico) → bloqueado por la respuesta de Mauri.
- **BUG-6** (cita reactivada) → arreglo independiente, no lo dispara este cambio.
- **BUG-7, BUG-8, BUG-9** → menores, pueden ir en el mismo PR o en uno de limpieza.

### Riesgo de no hacer nada

El cambio de SMD **ya está en producción**. Cada cita que se modifique en SMD va a mostrar la **fecha vieja** en el CRM durante la ventana del `--days`, y después se va a corregir sola. Es decir: **el CRM va a mostrar fechas de citas incorrectas de forma intermitente y no reproducible.** En una clínica dental, eso es un paciente que se presenta el día equivocado.

---

## Parte 6 — Qué se corrigió

**Fecha:** 2026-07-14 · **Estado:** aplicado y verificado contra Dropbox y la BD reales.

### Cambios

| Archivo | Cambio | Bug |
|---|---|---|
| `database/migrations/2026_07_14_120000_add_source_updated_at_to_smd_synced_events_table.php` | Columna `source_updated_at` + backfill desde `raw_payload` | BUG-1 |
| `Services/SmdAppointmentSyncService.php` | Descarte de payloads obsoletos por `updated_at`, orden cronológico de archivos, `processPayload()` público, lock por cita | BUG-1, BUG-5 |
| `Services/DropboxService.php` | `token()` ya no cachea fallos; `listChanges()` con `default:` | BUG-2, BUG-3 |
| `Jobs/ProcessDropboxNotification.php` | Delega en `processPayload()` (elimina la lógica duplicada), `ShouldBeUnique` | BUG-1, BUG-5 |
| `Services/SmdAppointmentSyncService.php` | La rama de cancelación aplica el contenido antes de cerrar el lead y guarda el `raw_payload` | BUG-12 |
| `app/Console/Kernel.php` | `withoutOverlapping(10)` en el cron | BUG-5 |
| `Console/Commands/SyncDropboxAppointments.php` | Reporta el contador `Obsoletos` | — |

**Decisión de diseño:** la corrección la garantiza el **descarte por `updated_at`**, no el ordenamiento. El orden por nombre de archivo es solo una optimización para evitar estados intermedios; si SMD cambia el formato del nombre, el descarte sigue protegiendo. Por eso `eventStatus` no se usa para decidir crear vs. modificar: el `_id` ya lo resuelve, y el descarte funciona incluso con los archivos legacy que no traen `eventStatus`.

`processPayload()` es ahora el único punto por el que pasan los tres orígenes del sync (comando, job del webhook, job de la UI), así que el descarte y el lock aplican a todos.

### Tests

`tests/Feature/SmdStalePayloadSyncTest.php` (nuevo, 5 tests) + 3 tests en `DropboxServiceTest.php`:

- La modificación de hoy gana sobre el archivo de creación de ayer (`--days=2`).
- El archivo viejo no revierte la cita aunque el cron corra muchas veces.
- Gana la versión más nueva aunque Dropbox devuelva los archivos en orden inverso.
- Los archivos legacy sin `updated_at` ni `eventStatus` se siguen procesando.
- Un legacy sin `updated_at` no borra la referencia guardada.
- Un refresh fallido no queda cacheado; al recuperarse Dropbox el sync vuelve solo.
- Un refresh sin `access_token` en la respuesta no se cachea.
- `listChanges` devuelve la estructura por defecto en vez de lanzar `TypeError`.

**Resultado:** 137 tests de Dropbox/SMD en verde. La suite completa queda en 314 pasando y **8 fallando por causas preexistentes ajenas a este cambio** (`AgentInsuranceVerifyTest`, `LeadReuseTest`, `LeadUnassignedOwnerTest`, `ReassignSyncedLeadsCommandTest`) — verificado haciendo stash de los cambios: fallaban igual antes.

### Verificación en producción

La cita `6a54f3524e365b0008cba3e3` (lead 3427, activity 37249) estaba corrida 4 días:

```
ANTES:  "Creado 21 de Jul"            | schedule_from = 2026-07-21 09:00   (payload __v=0)
DESPUÉS: "Updated from 16 to 17 de Jul" | schedule_from = 2026-07-17 09:00   (payload __v=5)
```

Backfill: los 1672 registros de `smd_synced_events` quedaron con `source_updated_at` poblado.

Corridas consecutivas de `smd:sync-dropbox --days=2` sobre los archivos reales:

```
1ª corrida: Creados: 0 | Actualizados: 3 | Cancelados: 0 | Sin cambios: 2 | Obsoletos: 0 | Errores: 0
2ª corrida: Creados: 0 | Actualizados: 0 | Cancelados: 0 | Sin cambios: 2 | Obsoletos: 3 | Errores: 0
```

En la segunda corrida los 3 archivos viejos se descartan como obsoletos y la cita se mantiene en 17-jul: **la oscilación desapareció**.

---

## Parte 7 — Segunda tanda: BUG-4 y BUG-11 (14/07)

Al retractarse BUG-10 (los ids sí cruzan), los dos bugs que estaban "bloqueados" se
resolvieron sin depender de SMD.

### Hallazgo previo: el `.env` apuntaba a la carpeta de pruebas

`DROPBOX_APPOINTMENTS_FOLDER` estaba en `/smd-events/69bd99417549b10008e0ab5f` (pruebas)
con la real comentada. Histórico en `smd_synced_events`: **1725** eventos de la carpeta real
vs **40** de la de pruebas. Se restauró la real y se re-ejecutó `smd:dropbox-init-cursor`
(el cursor del webhook queda atado a la carpeta con la que se creó).

### Cambios

| Archivo | Cambio | Bug |
|---|---|---|
| `IncomingAppointmentService::normalizeDropbox()` | El médico se identifica cruzando `attendances[]._id` contra `doctors.unique_id` (nuevo helper `knownDoctorIds()`), con `type` como fallback. **Se elimina el fallback al `owner`** y el método `parseOwner()` queda sin uso | BUG-4 |
| `IncomingAppointmentService::normalizeDropbox()` | Nuevas banderas `doctor_identified` / `patient_identified` | BUG-4, BUG-11 |
| `IncomingAppointmentService::updateDropbox()` | Re-resuelve médico y paciente y sincroniza `participants` + `doctor_activities`; reapunta el `person_id` del lead. Si el payload no los identifica, **conserva los actuales** | BUG-11 |
| `IncomingAppointmentService::resolvePerson()` | Respalda `smd_patient_id` cuando encuentra al paciente por teléfono, para que un `UPDATED` deshidratado no cree un duplicado | BUG-11 |

**Decisión de diseño:** identificar por `_id` y no por `type` vuelve al sync inmune a la
deshidratación de los `UPDATED`. El `owner` no se usa nunca: es quien creó el evento
(la recepcionista), no el profesional.

**Guardarraíl:** `updateDropbox()` solo reasigna participantes cuando puede identificarlos.
Sin esta condición, un payload sin médico reconocible pisaría al doctor correcto con el
placeholder "Doctor SMD" — un riesgo que **no existía antes**, porque el método no tocaba
participantes en absoluto.

### Tests

`tests/Feature/SmdParticipantsSyncTest.php` (nuevo, 4 tests) + 2 en `IncomingAppointmentDropboxTest.php`:

- Un `UPDATED` de una cita inexistente **la crea** (la decisión es por `_id`, no por `eventStatus`).
- Un cambio de paciente **lo registra** y reapunta la cita y el lead.
- Un cambio de médico reapunta la cita al doctor correcto.
- Un payload sin médico identificable **conserva** el doctor actual.
- `normalizeDropbox()` identifica al médico por `unique_id` aunque no venga `type`.
- `normalizeDropbox()` **no** usa el `owner` como doctor.

**Resultado:** 144 tests de Dropbox/SMD en verde. Suite completa: 321 pasando, los mismos
**8 fallos preexistentes** de siempre.

### Verificación en producción

```
Creados: 6 | Actualizados: 4 | Cancelados: 6 | Sin cambios: 257 | Obsoletos: 57 | Errores: 0
```

### ⚠️ Deuda: 19 citas siguen colgadas del doctor 458

El fix aplica a payloads nuevos o modificados. Las citas cuyo payload **no cambió** se
marcan `sin cambios` y **nunca se re-resuelven**, así que siguen colgadas de
"Recepcionista Odontoking" (`doctors.id = 458`). Hace falta un backfill que re-aplique el
`raw_payload` guardado en `smd_synced_events` para esas 19. Recién después se puede borrar
el doctor 458.

---

## Anexo — Cómo reproducir la inspección

```bash
php -r '
require "vendor/autoload.php";
$app = require "bootstrap/app.php";
$app->make(Illuminate\Contracts\Console\Kernel::class)->bootstrap();
config(["cache.default" => "array"]);
$svc = app(\Webkul\Admin\Services\DropboxService::class);
foreach (["2026-07-14","2026-07-13"] as $d) {
    foreach ($svc->listFilesForDate($d) as $f) {
        $j = $svc->downloadJson($f["path_lower"]);
        printf("%s | eventStatus=%s | %s | %s\n", $f["name"], $j["eventStatus"] ?? "(no trae)", $j["summary"], $j["startDate"]);
    }
}'
```

> ⚠️ `php artisan tinker` **no funciona desde el entorno de desarrollo** para consultas a BD: el host `heaven_odontoking-bd` no resuelve. La inspección de Dropbox sí funciona (es internet público). Los tests corren contra la BD de testing.

**Screenshot original de Mauri:** `WhatsApp Image 2026-07-13 at 18.07.40.jpeg` (sin trackear en git).

---

## Referencias

- `planning/00-sharemedata-api-v2/00.agent-task.md` — plan v2 original (**desactualizado**: Fases 1, 3 y 4 ya están implementadas)
- `planning/05-sincronizacion-citas-dropbox/00.preguntas-pendientes.md` — decisiones previas (**Pregunta 3 queda invalidada por BUG-4**)
- `planning/05-sincronizacion-citas-dropbox/03.mapeo-doctor-smd.krayin-developer.md` — mapeo doctor SMD → CRM
- `planning/06-webhook-dropbox/` — arquitectura del webhook
